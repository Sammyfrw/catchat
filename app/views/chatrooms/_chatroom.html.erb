<script>
  window.PASSKEY = "<%= chatroom.passkey %>";
  window.PASSKEY_PROMPT = "Please enter the passkey for <%= chatroom.name %>";
  window.PATH = "chatrooms/<%= chatroom.id %>"
</script>

<li>
  <%= chatroom.name %>

  <form class="enter-chatroom">
    <input type="button" value="Enter Chat" onClick="validatePasskey()">
  </form>
</li>
